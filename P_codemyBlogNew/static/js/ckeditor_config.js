CKEDITOR.editorConfig = function(config) {
    // Set the maximum width of the CKEditor
    config.width = '900px';

    // Adjust the width when the viewport is below the maximum width
    config.autoGrow_maxWidth = '100%';

    // Other configuration options...
    // config.xxx = ...;
};
